<%= form_tag("/products/#{@product.id}/newcategory", method: "post") do %>

  <% @categories.each do |category| %>
    <%= check_box_tag("category[]", category.id) %>
    <%= label_tag(category.name) %>
  <% end %>
<%= submit_tag("Submit") %>
<% end %>
